# rust_ollama_code_examples

# reqwest = HTTP client

# blocking feature = sync calls (no async)

# json feature = easy JSON handling

# serde = convert JSON ↔ struct

# derive = so we can write #[derive(Deserialize)]
